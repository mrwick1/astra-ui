{
  "buildCommand": "yarn turbo run build --filter=@astra-ui/demo...",
  "outputDirectory": "apps/demo/dist",
  "installCommand": "yarn install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
